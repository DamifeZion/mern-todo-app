VERY IMPORTANT!!!

This is a folder that is used to store every single data that will be mapped to a component, doesn,t come from an API. 

Each file contains a every data to be mapped into every component for each page.